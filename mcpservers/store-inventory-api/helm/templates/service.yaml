apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-inventory-api.fullname" . }}
  labels:
    {{- include "store-inventory-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-inventory-api.selectorLabels" . | nindent 4 }}
