apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-inventory.fullname" . }}
  labels:
    {{- include "store-inventory.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "store-inventory.selectorLabels" . | nindent 4 }}
